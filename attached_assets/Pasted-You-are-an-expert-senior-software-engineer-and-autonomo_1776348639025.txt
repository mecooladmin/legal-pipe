You are an expert senior software engineer and autonomous Replit build agent. Your task is to generate a complete, production-ready repository from scratch.

You must build a **low-RAM, high-efficiency document intelligence system with a user-friendly UI** for end users.

The system processes 743 mixed documents (PDF, images, Excel, text), performs OCR, indexing, semantic search, and AI Q&A using a fully local pipeline.

---

# HARD CONSTRAINTS (NON-NEGOTIABLE)

* Must run in **1–2GB RAM environments**
* Must not load multiple files into memory
* Must support **resume after interruption**
* Must persist ALL intermediate outputs to disk
* Must be optimized for Replit execution limits
* Must avoid expensive computation loops
* Must process data incrementally (streaming only)

---

# FINAL PRODUCT REQUIREMENTS

You must generate a complete project with:

## 1. BACKEND PIPELINE

* File ingestion queue system (resume-safe)
* PDF text extraction (PyMuPDF)
* OCR fallback pipeline (PaddleOCR → Tesseract)
* Excel → CSV conversion
* Streaming text normalization
* Chunking (500–800 words)
* Embeddings (sentence-transformers all-MiniLM-L6-v2)
* FAISS vector database (persistent on disk)
* Local LLM integration via Ollama (Mistral or LLaMA3)
* RAG query engine (retrieval + grounded answers)

---

## 2. ULTRA-LOW RAM DESIGN RULES

You MUST:

* Process ONE file or ONE page at a time
* Immediately write results to disk
* Never store full dataset in memory
* Use batch size ≤ 8 for embeddings
* Use FAISS IndexFlatL2 only (no training indexes)
* Persist FAISS index every 25 embeddings
* Explicitly delete objects after use (gc.collect())

---

## 3. FAST OCR PIPELINE

Implement tiered OCR:

### Level 1 (fastest)

* Extract embedded PDF text via PyMuPDF

### Level 2

* PaddleOCR (CPU mode, no angle classifier, batch size 1)

### Level 3 fallback

* Tesseract minimal config

Preprocess images:

* resize max width 1280px
* grayscale only
* no heavy enhancement filters

---

## 4. STORAGE STRUCTURE

Create this exact structure:

```
/data
/processed/raw_text
/processed/chunks
/vector_store
/outputs/summaries
/outputs/entities
/outputs/timeline
/logs
```

---

## 5. RESUME SYSTEM (CRITICAL)

Implement:

* queue.jsonl for file processing state
* checkpoint after each file/page
* ability to restart without duplication
* skip already processed items

---

## 6. RAG ENGINE

Flow:

1. user query → embedding
2. FAISS top 5 retrieval only
3. inject minimal context into LLM
4. return:

   * answer
   * citations (file + page)

---

## 7. LOCAL LLM

Use Ollama:

* preferred model: mistral (quantized if possible)
* strict context trimming
* no large prompt injection

---

# 8. END USER UI (STREAMLIT)

Build a clean, simple UI:

## UI MUST INCLUDE:

### PAGE 1: Dashboard

* total files processed
* progress bar
* system status (OCR / embeddings / ready)

---

### PAGE 2: Document Search (RAG)

* chat-style interface
* ask questions over all documents
* show:

  * answer
  * cited sources
  * expandable references

---

### PAGE 3: Document Explorer

* filter by file type
* view extracted text
* view chunks
* download raw/cleaned outputs

---

### PAGE 4: Insights

* auto-generated:

  * timeline
  * entities
  * summaries
* export buttons (JSON / Markdown)

---

### UI REQUIREMENTS:

* fast loading (no heavy frontend frameworks)
* Streamlit only
* lazy loading of data
* no full dataset rendering at once

---

## 9. PERFORMANCE OPTIMIZATION

You MUST:

* prioritize RAM efficiency over speed
* use streaming everywhere
* avoid multiprocessing except OCR (max 2 workers)
* cache aggressively to disk
* never recompute completed steps

---

## 10. LOGGING & DEBUGGING

* log every stage:

  * ingestion
  * OCR
  * chunking
  * embedding
  * query
* store logs in `/logs/app.log`

---

## 11. OUTPUT DELIVERABLES

Generate:

* full working repo
* requirements.txt optimized for low RAM
* main.py entrypoint
* streamlit_app.py UI
* modular backend pipeline
* README with run instructions

---

## 12. SUCCESS CRITERIA

The system is successful if:

* It runs fully inside Replit
* It processes all 743 files without crashing
* It can resume after interruption
* OCR pipeline completes reliably under low RAM
* UI allows end users to search and explore documents easily
* All outputs are traceable and cited

---

# FINAL INSTRUCTION

Generate the complete repository structure AND all required code. Prioritize simplicity, memory safety, and reliability over complexity.

Begin with project scaffold, then implement backend, then UI, then integration.
